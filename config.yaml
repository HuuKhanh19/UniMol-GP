# UniMol-GP -- Tunable hyperparameters (finetune here)
# Priority: CLI > config.yaml > script defaults

# Shared (all steps)
split_seed: 4
n_confomer: 1
gpu_id: 0

# Step 1 -- Gradient Descent
epochs: 100
batch_size: 32
learning_rate: 0.0001
patience: 10