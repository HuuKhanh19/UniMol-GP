# FreeSolv Dataset Configuration

dataset:
  name: "freesolv"
  file: "refined_FreeSolv.csv"
  smiles_column: "smiles"
  target_column: "measured"
  task_type: "regression"
  metric: "rmse"

# EGGROLL settings for this dataset (optional overrides)
# Full-batch mode: uses entire training set (~508 samples)
# No batch_size needed - removed for full-batch training
  
# Expected baseline performance
expected:
  rmse: 1.50