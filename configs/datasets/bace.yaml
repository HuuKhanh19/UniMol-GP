# BACE Dataset Configuration

dataset:
  name: "bace"
  file: "refined_BACE.csv"
  smiles_column: "smiles"
  target_column: "class"
  task_type: "classification"
  metric: "auc"

# EGGROLL settings for this dataset (optional overrides)
# Full-batch mode: uses entire training set (~1210 samples)
# No batch_size needed - removed for full-batch training
  
# Expected baseline performance
expected:
  auc: 0.82