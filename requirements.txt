# UniMol-GP Requirements
# The remaining deps (torch, joblib, addict, numba, ...) come from
# unimol_source/setup.py -- install it with:  pip install -e unimol_source

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
pyyaml>=6.0

# PyTorch: install separately, matching your CUDA version
# torch>=1.10.0

# Chemistry
rdkit>=2022.03.1

# Logging and utilities
tqdm>=4.62.0
